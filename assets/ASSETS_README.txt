Assets for Psylocyba Tools (fonts, icons, flags).

To create them: run in project root:  create_resources.bat
Or:  python create_assets.py

Creates: assets/fonts/ (Inter), assets/icons/ (key, qr, refresh, trash, info), assets/flags/ (ru, gb).
Requires: Python, Pillow (pip install Pillow).
